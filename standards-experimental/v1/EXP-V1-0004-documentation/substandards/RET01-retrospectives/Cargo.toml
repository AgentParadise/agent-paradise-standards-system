[package]
name = "documentation-retrospectives"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Retrospectives substandard: enforces append-only retrospective history with consistent naming and required sections"

[dependencies]
documentation = { path = "../.." }
aps-core.workspace = true
regex = "1"

[dev-dependencies]
tempfile = "3.14"
