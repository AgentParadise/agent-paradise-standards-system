schema = "aps.experiment/v1"

[experiment]
id = "EXP-V1-0005"
name = "Agent Recipe Standard"
slug = "agent-recipe"
version = "0.2.0"
category = "technical"
backwards_compat = false  # Directory shape (0.2.0) is a breaking change from the 0.1.0 single-file schema

[aps]
aps_major = "v1"

[ownership]
maintainers = ["AgentParadise"]

# Promotion metadata (added after promotion, do not edit manually)
# [promotion]
# promoted_to = "APS-V1-XXXX"
# promoted_at = "YYYY-MM-DD"
