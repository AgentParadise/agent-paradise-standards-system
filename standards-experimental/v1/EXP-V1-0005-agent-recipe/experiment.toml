schema = "aps.experiment/v1"

[experiment]
id = "EXP-V1-0005"
name = "Agent Recipe Standard"
slug = "agent-recipe"
version = "0.3.0"
category = "technical"
backwards_compat = false  # 0.3.0 renames agent to harness, makes tools an enforced allowlist, and restricts from: inheritance to narrowing only; all are breaking changes from 0.2.0

[aps]
aps_major = "v1"

[ownership]
maintainers = ["AgentParadise"]

# Promotion metadata (added after promotion, do not edit manually)
# [promotion]
# promoted_to = "APS-V1-XXXX"
# promoted_at = "YYYY-MM-DD"
