# Agentic Workspace Examples

`minimal/workspace-launch.json` is a complete valid launch manifest for the
explicitly insecure Local provider profile. It includes every major boundary:
content, model intent, skills, tool policy, transcript capture, outputs, and
resource limits.
