# DEP01 Examples

- `example-approved-deps.toml` — minimal approved list demonstrating both categories (lands in Commit 2).
- `violation-fixture/` — a fake crate with an unapproved dep, used by `rule_evaluation.rs` (lands in Commit 2).
