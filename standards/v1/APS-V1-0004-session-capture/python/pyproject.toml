[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "apss-session-capture"
version = "2.1.0"
description = "APS-V1-0004 portable workflow inventory models"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.10,<3"]

[tool.setuptools.package-data]
apss_session_capture = ["py.typed"]
